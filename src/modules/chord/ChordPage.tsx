import React from 'react';
import { guitar } from '../../hooks/chord-db';

const ChordPage = (): JSX.Element => {
  console.log('chords', guitar);
  return <div>chords</div>;
};

export default ChordPage;
